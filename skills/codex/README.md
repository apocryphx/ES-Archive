# Codex skills for ES Archive

Codex's suite for the ES Archive MCP tools. Owned and edited by Codex; Claude
does not edit files here (see `AGENTS.md` / `CLAUDE.md` at the repo root).

Deploy with `scripts/sync-skills.sh`, which copies each skill folder to
`~/.codex/skills/<name>/`.
