# macOS
.DS_Store

# Xcode — user-specific state
xcuserdata/
*.xcuserstate
project.xcworkspace/xcuserdata/

# Xcode — build products
build/
DerivedData/

# Embedder assets — the EmbeddingGemma model + tokenizer live in the git
# submodule ES_Archive/Embedders/embeddinggemma-300m-qat-q4_0-coreml (hosted
# on Hugging Face, LFS). Restore with: git submodule update --init

# Help indexes — regenerated by the hiutil build phase on every build
*.helpindex

